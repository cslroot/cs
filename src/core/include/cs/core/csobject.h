#pragma once

#include <cs/core/core_common.h>

namespace cs
{
    namespace core
    {
        class DECL_CS_CORE CSObject
        {
        public:
            CSObject();
            virtual ~CSObject();
        };
    } // namespace core

} // namespace cs
