#pragma once

#include <cs/render/render_common.h>

namespace cs
{
    namespace render
    {
        class DECL_CS_RENDER renderer
        {
        private:
            /* data */
        public:
            renderer(/* args */);
            ~renderer();
        };

    } // namespace render

} // namespace cs
