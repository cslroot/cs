#pragma once

#include <cs/render/render_common.h>

namespace cs
{
    namespace render
    {
        class DECL_CS_RENDER Scene
        {
        private:
            /* data */
        public:
            Scene(/* args */);
            ~Scene();
        };
    } // namespace render

} // namespace cs
